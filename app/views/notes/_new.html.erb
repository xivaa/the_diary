<%= simple_form_for @note, html: {
                    data: {
                      insert_in_list_target: "form",
                      action: "submit->insert-in-list#send"
                    }} do |f| %>
  <%= f.input :text, as: :text %>
  <%= f.button :submit %>
<% end %>
